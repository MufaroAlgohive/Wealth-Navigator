import { PortPlaceholder } from "@/components/admin/port-placeholder";

export default function Page() {
  return (
    <PortPlaceholder
      title="Order Book"
      parityRef="D2 — Order Book"
      summary="Live positions, daily snapshots, CSV export, Strate BIR & fills."
    />
  );
}
