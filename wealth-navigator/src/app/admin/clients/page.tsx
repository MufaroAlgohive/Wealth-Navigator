import { PortPlaceholder } from "@/components/admin/port-placeholder";

export default function Page() {
  return (
    <PortPlaceholder
      title="Clients"
      parityRef="C — Clients"
      summary="Client roster, KYC review, documents, holdings & activity timeline."
    />
  );
}
