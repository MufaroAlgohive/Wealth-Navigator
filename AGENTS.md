## Learned User Preferences

- IRESS Web Services credentials (`IRESS_USERNAME`, `IRESS_PASSWORD`, `IRESS_COMPANY_NAME`) are server-side SOAP session creds only — never application user login or browser pre-fill
- Replace dev `admin`/`admin` app login with Supabase Authentication for production users; `/oems` is the main post-login entry (not a marketing landing); login must not show demo persona cards or institutional marketing pitch blocks
- Theme: purple accent on white (light) and dark slate-purple (dark); OEMS glassmorphism via `globals.css` tokens + shared `oems/primitives/glass.tsx` (`GlassSection`, `GlassKpi`, `PageCanvas`, `glass-inset`) across desk + persona pages
- Prefer basic IRESS endpoint lists (method names grouped by namespace) over verbose rationales for email attachments; IRESS follow-up emails to Charles should be reply-toned on the existing thread, not cold first-contact
- Do not run local live IRESS or set `IRESS_MODE=live` on Vercel; Vercel reads `IRESS_MODE=mock` + `USE_SUPABASE_QUOTES=true` (Path A: DB-first from worker-ingested Supabase quotes); Railway `Iress-Worker-PROD` is the only `IRESS_MODE=live` seat against `webservices.iress.co.za/v4`. The two-server split (Vercel mock + Railway live) is the production posture
- Clear LIVE/MOCK/SEED/SUPABASE/STREAM/UNCONFIGURED/WORKER labels and no fake/seed data on production pages (including Canvas); honest empty states when source is unconfigured; paper-model dashboards must show the full realized+unrealized set with daily P&L, drawdown, fees, and a chart that visually separates the account series from the benchmark (e.g. STX40)
- Investment Committee voting membership is fixed to 3 people (Lonwabo, Juan, Lethabo) and a 2/3 majority passes (≥ 2 yes of 3) — committee roster + threshold live in `lib/research-ic/committee.ts` and the server-side gate in `lib/research-ic/committee-gate.ts`; both research-note votes and rebalance votes must respect the whitelist and the 0.5 threshold; the 3-admin discovery query lives at `supabase/seeds/ic_committee_institutional.sql` (line 59+) and `committee_member_c` on the institutional DB is the durable whitelist
- Wants explicit opt-in for LIVE writes (worker defaults `IRESS_WORKER_DRY_RUN=1` and `SUPABASE_ALLOW_WRITES=0`; production needs both flipped for quote + `worker_session_metadata` writes); end-to-end UAT runs on MINT_CT IOSPlus UAT account `56378` only — UAT and production IRESS seats must stay strictly isolated, no real client money
- Production go-live step 1: Railway `Iress-Worker-PROD` connected to IRESS production with endpoints verified before dashboard/UI work; the former UAT Railway worker was deleted for production-only operation; order lifecycle must surface every IRESS state (rejected, at-desk, at-Longmog, partial, filled, cancelled, etc.), not just working/complete — user wants to see exactly where an order is stuck if it stalls
- Default code posture is IRESS production (2026-07-23 cutover): `lib/iress/index.ts:74` defaults `IRESS_BASE_URL=https://webservices.iress.co.za/v4`, `lib/iress/index.ts:230` defaults `IRESS_IOS_SERVER=MINT`, `lib/oems/uat-scope.ts:isUatEnv()` returns false when env is unset (was: true). UAT defaults only fire when `IRESS_UAT_MODE=1` or `IRESS_BASE_URL=webservices-ct.*` is explicit. `lib/data-policy.ts:isUseSupabaseQuotesEnabled()` now defaults true so unset prod env reads Supabase (Path A) instead of falling back to IRESS/mocked. Worker `env.ts:newsVendorCode` default is `SENSD` (the only vendor the prod market-data seat is entitled to; `SENS` real-time is not on prod). `IRESS_ACCOUNT_CODE` is **required** at runtime — `api/orders/{preflight,submit}` and `api/admin/orderbook/send-to-market` return 503 when unset instead of silently defaulting to `56378` (UAT). Production worker account is `43448`
- Only one IRESS license seat exists for all IRESS services — the Railway worker alone may hold it; never open competing live IRESS sessions from Vercel, local, or retail app
- Retail gift purchases must validate cost against live IRESS market prices and only deduct wallet / mark funded after the order is placed, confirmed, and filled (not on authorize alone); OEMS Canvas under Research & IC is labeled "Canvas (beta)" — spacious pan/zoom workspace with real MiniMax + IRESS + Yahoo data only (Fynca-like multi-persona canvas; no fake nodes)

## Learned Workspace Facts

- Next.js app lives in `wealth-navigator/`; root `package.json` delegates `npm run dev` via `npm --prefix wealth-navigator`; deploy from repo root: `vercel --scope autonama-group --prod` against project `autonama-group/wealth-navigator` (Root Directory `wealth-navigator`); rebuilt from Lovable Codebase (Vite/React) to Next.js 16 + React 19 + Bun + TypeScript strict + Tailwind + shadcn/ui
- Stack: Vercel (Next.js 16 + Bun; prod `wealth-navigator-one.vercel.app`) + Supabase (auth/DB/audit/realtime quotes; production topology is **3 DBs — see [docs/DB_TOPOLOGY_DECISION.md](wealth-navigator/docs/DB_TOPOLOGY_DECISION.md)** (decided 2026-06-13; supersedes the two `TWO_DB*` memos): RETAIL prod `mfxnghmuccevsxwcetej` = customer books + shared price tables `securities_c`/`stock_intraday_c`; INSTITUTIONAL prod `nnwzhxfjpjbzujevwzlh` (promoted from test; MCP `user-supabase - MyMint`) = desk trading book + desk-only analytics (orders/positions/bookings/cash/FIX+/curves/indices/sectors/macro/news/bonds/MM); STAGING = a fresh project (validate IRESS + migrations before prod). Compliance requires institutional≠retail in separate DBs. Worker uses two service-role clients (prices→retail prod, trading/analytics→institutional prod); BFF reads both prods; keys in gitignored `supabase_creds`; env split `RETAIL_SUPABASE_*` / `INSTITUTIONAL_SUPABASE_*` / `STAGING_SUPABASE_*`; **cutover pre-req:** identify existing ~15s writer of `mfxng…stock_intraday_c` before worker takeover; client↔account bridge (`mint_number`↔IRESS `AccountCode`) deferred past OEMS v1) + Railway `Iress-Worker` (service name in Railway UI; directory `workers/iress-ingest/`; Dockerfile `workers/iress-ingest/Dockerfile`; worker `tsconfig.json` needs `baseUrl: "../.."` for `@/` in Docker; single replica holds CT/prod seat; DB-first ingest; public domain `https://iress-worker-production.up.railway.app` with HTTP API on port 8765; **Railway MCP `mcp.railway.com` is registered** for projects/services/deployments/logs/redeploy)
- Product vision and IRESS reference docs live under `Documentation & Vision/` (`iress-v4-docs`, `Email`, programmers guide PDF); IRESS email deliverables include `mint-iress-email.txt` plus basic endpoint lists (`mint-iress-v4-endpoints-*-basic.txt`); stack/data-source docs: `wealth-navigator/docs/STACK_ARCHITECTURE.md`, `docs/DATA_PROVENANCE.md`, `docs/REMAINING_GAPS.md`; go-live runbook `docs/MINT_GO_LIVE_RUNBOOK.html`; Vercel setup `wealth-navigator/docs/VERCEL_DEPLOY_SETUP.md`
- BFF API routes under `src/app/api/`: `/api/orders` (Supabase-backed), `/api/orders/live` (worker passthrough), `/api/quotes` (DB-first when `USE_SUPABASE_QUOTES=true`), `/api/research-lab` (retail `strategies_c` research payloads), `/api/worker-health` and `/api/integration/health` (worker heartbeat), plus IRESS session/legacy `/api/iress/{health,provenance,quotes,session}` and `/api/ticks` SSE; UI must call these, never raw IRESS SOAP; **Path A** Worker→Supabase→BFF for snapshots/audit (`/api/quotes`, `/api/orders`, `/api/worker-health`), **Path B** Worker→Vercel passthrough for live/ephemeral (`/api/orders/live`, `/api/orders/stream`, `/api/integration/health`) gated by `IRESS_WORKER_URL` + `WORKER_HTTP_TOKEN`. JSE All Share intraday (`/api/indices/J203`) must be IRESS-sourced (not Yahoo); Strategies returns should reflect live performance, not stale rebased series alone
- Data tiering T0–T6: T0 reference/master (`securities_c`), T1 authoritative snapshots (`stock_intraday_c` worker upsert + Realtime push), T2 display-only ticks (ephemeral SSE; do not persist), T3 orders/audit (`oems_order_audit`), T4 books/P&L, T5 vendor content (SENS/news/macro — seed until contracted), T6 synthetic/demo (PCA, fake depth). Yahoo fallback path for IRESS outage is wired via the `/api/cron/yahoo-fundamentals` cron: setting `YAHOO_FUNDAMENTALS_WRITE=1` + leaving `IRESS_STALE_FALLBACK_HOURS` unset (or `0`) flips `yahooOwnsPrice=true` for every symbol — `approved-symbols.ts` is **fail-closed** on empty `iress_price_validation_c` so `yahooOwnsPrice` defaults true; `/api/indices/J203` already has `^J203.JO` Yahoo fallback; `/api/news` falls back to Moneyweb + BusinessTech RSS when SENS is offline; `/api/orders/preflight` has a local preflight path that runs without the worker
- OEMS trading desk is the primary product surface; IRESS V4 SOAP integration is the technical centerpiece; **Research Lab** at `/oems/research-lab` (Info nav + ⌘K palette) mirrors the Lovable Yield Basket prototype — BFF `/api/research-lab` over retail `strategies_c` + `securities_c` prices/Yahoo fundamentals; thesis add/remove proposal workflow is session-only (no DB persistence yet); **Research Library** at `/oems/research-library` hosts the multi-step New Note wizard (`components/research-ic/note-editor.tsx`) — Step 1 Target Price field shows a pulsing live-IRESS mark badge with signed upside % vs target via `/api/company-analysis/[sym]` (60s poll, 30s staleTime, sourced from IRESS overlay or `stored`); Review tab (Step 6) also renders `Target R{price} · now R{price} · upside {±n%}`; **Canvas (beta)** lives under Research & IC (`/oems/canvas`) — Fynca-inspired visual multi-persona workspace using MiniMax + IRESS + Yahoo; Lovable preview URLs are auth-gated — implement from requirements/description
- IRESS adapter switches mock vs live via `IRESS_MODE`; live SOAP targets production `webservices.iress.co.za/v4` (UAT was `webservices-ct.iress.co.za/v4`) with 17-method `IressClient` surface; `IRESSSessionStart` uses separate `UserName` + `CompanyName` (use `parseIressUserCode()` in `src/lib/iress/config.ts` to split `user@company`); `ServiceSessionStart` (IOS+/IPS/FIX+) HTTP 500 is non-fatal — quote ingest uses `PricingQuoteGet` with `IRESSSessionKey`; only Railway worker runs `IRESS_MODE=live`
- IRESS service routing per Andre (Mint/CT, confirmed 2026-07-09 via working SOAP envelopes): Market Data + News → **Iress** service; Orders/Users/Accounts/Limits → **IOSPlus** service; production uses `https://webservices.iress.co.za/v4`, IOSPlus server `MINT`, and delayed-news vendor `SENSD` (not `SENS`); `TimeSeriesGet2` is now WORKING with `DataSource=zax` + `Exchange=jse` (unblocks tier-2 panels: ALSI/J203, sector heatmap, ZAR sovereign curve); `OrderCreate3` is now WORKING on MINT_CT (Andre created a test SOL pending order). Quote row shape variance: NPN sends bare `<Last>`; AGL/FSR/MTN/SBK/BHG send `LastPrice`/`PreviousClosePrice` integer cents; worker `mapQuote`/`resolveQuoteLast` validates `Last` against the `Close` anchor and **skips writes** when only a stale `LastPrice` is present (BHG pattern); for new entitlement probes use the worker's `/debug/timeseries-probe` and record the working param shape in the request notes
- IRESS is single-seat for the entire product (market data + orders + news); SOAP fault 25008 = no licenses; proper logout is `ServiceSessionEnd` × N → `IRESSSessionEnd` → 3s (`LICENSE_RELEASE_DELAY_MS`); recovery via `bun run iress:logout` or `IRESS_FORCE_KICK_ALL=1`; worker auto-kicks first-boot 25008 when `worker_session_metadata` has no `iress_session_key`; stop Railway service (scale-to-0 unavailable on some plans) to release seat; **never put IRESS passwords or API keys in `AGENTS.md`, client bundles, or `NEXT_PUBLIC_*` variables**; `IRESS_RESET_ON_BOOT=1` is the deploy-time seat-recovery switch, **ON BY DEFAULT since 2026-08-17** — on every Railway push the new container wipes its `worker_session_metadata` row and sends `SessionNumberToKick=-1` on the FIRST `IRESSSessionStart` so it claims the seat before the old replica can settle back into it; deploys no longer need an operator to flip the env. **Trade-off:** a deploy WILL kick any other live `DFM@Mint` session including Charles's IRESS Chrome / CT terminal — intentional, deploy reliability > Charles's local IRESS view during the ~2 min Railway blue-green swap. To opt out: set `IRESS_RESET_ON_BOOT=0` on Railway (not recommended).
- All Supabase migrations are review-only and user-pasted in SQL editor (worker never auto-applies DDL on LIVE); idempotent SQL — institutional OEMS DDL in `supabase/migrations/` (nnwz), retail price DDL in `supabase/retail/`; worker ingest targets `securities_c` + `stock_intraday_c` (cents, `supabase_realtime`); `IRESS_RETAIL_INGEST` only writes `securities_c.last_price` + `stock_intraday_c`, double-gated by `IRESS_RETAIL_DRY_RUN`; sticky `ApplicationID` + session key in `worker_session_metadata`; `USE_SUPABASE_QUOTES=true` routes UI quote reads to Supabase
- Retail live app is `https://app.mymint.co.za` (MINT-DEVELOPMENT / MINT-LIVE). **Gifting** on registry wishlists must use IRESS-validated pricing, park gifts to the institutional OEM/orderbook via the worker, and keep funding status accurate (refund failed/unfilled gifts; do not leave “fully funded” when no fill). Broker fill path and settlement still need ongoing verification — C4 `workers/broker-ingest/` and C6 Ozone wallet remain mock-only pending Lonwabo/Tsie contracts; C7 Finance at `/admin/finance` aggregates AUM fees (RETAIL `client_strategy_returns_c`) and Day-1 P&L (INSTITUTIONAL `oems_order_audit`)
- JSE Alpha paper model (Qentari Bravo JSE) is a strategic workstream: long-only JSE equity, ~7 concentrated names, weekly rebalance, benchmark STX40.JO, ZAR, R500k budget; data source `yahoo`, prices pushed into retail `securities_c` + `stock_intraday_c`; live demo view at `/oems/models/jse_alpha` (Paper account tab) reads via `/api/models/[id]/benchmark`; external pusher lives at `E:\Autonama\Active Projects\Algos\Autonama_Algo\Qentari Models\Qentari_Bravo_JSE\docker-compose-pusher.yml` (docker compose up)
