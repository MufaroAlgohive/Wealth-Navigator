{
  "name": "wealth-navigator-root",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "npm --prefix wealth-navigator run dev",
    "build": "npm --prefix wealth-navigator run build",
    "start": "npm --prefix wealth-navigator run start",
    "lint": "npm --prefix wealth-navigator run lint",
    "format": "npm --prefix wealth-navigator run format",
    "test": "npm --prefix wealth-navigator run test",
    "test:watch": "npm --prefix wealth-navigator run test:watch",
    "test:e2e": "npm --prefix wealth-navigator run test:e2e",
    "typecheck": "npm --prefix wealth-navigator run typecheck",
    "dev:install": "npm --prefix wealth-navigator install",
    "dev:reset": "rm -rf wealth-navigator/.next && npm --prefix wealth-navigator run dev"
  }
}
